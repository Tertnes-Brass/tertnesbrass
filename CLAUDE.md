# Claude Code Context – Tertnes Brass Website

The canonical agent instructions are in [`AGENTS.md`](./AGENTS.md).

This is a statically generated Astro site using TypeScript, Bun, custom CSS and
Cloudflare Workers Static Assets. Run `bun run build` before handing off changes
and verify both light and dark themes.
